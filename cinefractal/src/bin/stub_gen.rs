//! Stub generator binary.
//!
//! Regenerates `cinefractal.pyi` from the Rust sources, including the `///`
//! doc comments as Python docstrings. Run with:
//!
//!     cargo run --bin stub_gen
//!
//! The resulting stub is picked up automatically by maturin and bundled into
//! the wheel.

use pyo3_stub_gen::Result;

fn main() -> Result<()> {
    // `stub_info` is generated by `define_stub_info_gatherer!` in lib.rs.
    // Locally disabled  for CI.
    /*
    let stub = cinefractal::stub_info()?;
    stub.generate()?;
    */
    Ok(())
}
